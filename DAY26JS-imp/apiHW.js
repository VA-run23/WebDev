let url="https://potterdb.com/?ref=apislist.com/quotes";

// fetch(url).then((data)=>console.log(data.quoteText));
fetch(url).then(()=>{
    let fact=data.quoteText;
    console.log(data.quoteText);
})
